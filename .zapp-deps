# Zapp sibling repository dependency lockfile.
# Mirrors the convention in zodl-android/.zapp-deps.
#
# secant.xcodeproj references ../zappMessaging/ios as a LOCAL Swift package
# (XCLocalSwiftPackageReference), so the sibling checkout is a build prerequisite,
# not an optional convenience. CI checks out this exact SHA; a fresh clone must run
# Scripts/bootstrap-zappmessaging.sh before the project will resolve.
#
# The package's binary artifacts (Addons/*.xcframework) and Resources/worklet.bundle
# are GENERATED and gitignored — `npm run setup` produces them from committed npm
# prebuilds (no native compilation). They are also VERSION-LOCKED to each other:
# worklet.bundle names each addon by exact version, so running `npm run link` without
# `npm run build` yields a runtime "No addon registered", not a build error. Always
# run `npm run setup`.
#
# Sourced from the PUBLIC SDK repository, so a clone of this project builds without
# any credential. The private JustZappIt/zappMessaging carries an extra store/city
# room feature that this app does not use; if that is ever ported to iOS, this pin
# has to move back to the private repo and CI needs a read token again.
#
# The sibling directory keeps the name `zappMessaging` because secant.xcodeproj
# references ../zappMessaging/ios by path.
#
# GitHub: https://github.com/JustZappIt/zappmessaging-sdk
zappMessaging=a8b88ed4f6cc22382a4c79167340031aa12c42b1

# zcash-swift-wallet-sdk — sibling LOCAL Swift package; run Scripts/bootstrap-zcash-sdk.sh
# after a fresh clone. Build with NO --slipstream. Pinned by SHA because the branch is
# unmerged upstream. Retire once upstream cuts 2.9.0; the released artifact is clean by default.
#
# The pin is branch `zapp/sdk-mit-on-main`: SDK origin/main with our 10 fork commits
# replayed on top. Two properties it has to keep, both checked by
# Scripts/verify-zcash-sdk-artifact.sh and the purity gates in the bootstrap script:
#
#   1. MIT-clean by default. The AGPL-3.0-only zodl-slipstream crate sits behind the
#      optional `slipstream` cargo feature, which we never enable, and is absent from the
#      default dependency graph entirely.
#   2. No credential needed to build. We deliberately do NOT carry upstream's
#      [patch.crates-io] entry pointing slipstream-core at the PRIVATE
#      zodl-inc/slipstream-internal repo — cargo fetches git dependencies while resolving,
#      before feature selection, so that entry would make every build (and CI job) require a
#      token for a repo we cannot read. We resolve zodl-slipstream 0.1.1 from crates.io
#      instead, which is why the authorize-slipstream CI action stays deleted on our line.
#
# Consequence, accepted: the `slipstream` feature itself may no longer build against this
# line's newer librustzcash generation. We do not ship that variant.
#
# GitHub: https://github.com/JustZappIt/zcash-swift-wallet-sdk
zcashSwiftWalletSdk=c2fa72d897bfe9aff1087aa2f7543de7ad2e9148
